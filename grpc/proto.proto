syntax = "proto3";

package proto;

option go_package = "ChittyChat/grpc/proto";

message Message {
    string msg = 1; 
    int32 lamport = 2; 
}

message Empty {

}

service ChittyChat {
    rpc SendMessage (Message) returns (Empty);
}
